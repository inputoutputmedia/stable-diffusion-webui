import launch

# # TODO: add pip dependency if need extra module only on extension

# if not launch.is_installed("opencv-python"):
#     launch.run_pip("install opencv-python", "requirements for objectsAndCaptionsDetection")

# if not launch.is_installed("huggingface_hub"):
#     launch.run_pip("install huggingface_hub", "requirements for objectsAndCaptionsDetection")

# if not launch.is_installed("tokenizers"):
#     launch.run_pip("install tokenizers==0.15.2", "requirements for objectsAndCaptionsDetection")

# if not launch.is_installed("transformers"):
#     launch.run_pip("install transformers==4.39.2", "requirements for objectsAndCaptionsDetection")

# if not launch.is_installed("numpy"):
#     launch.run_pip("install numpy", "requirements for objectsAndCaptionsDetection")

# if not launch.is_installed("ultralytics"):
#     launch.run_pip("install ultralytics", "requirements for objectsAndCaptionsDetection")

# if not launch.is_installed("mysql-connector-python"):
#     launch.run_pip("install mysql-connector-python", "requirements for objectsAndCaptionsDetection")

# if not launch.is_installed("typing"):
#     launch.run_pip("install typing", "requirements for objectsAndCaptionsDetection")

if not launch.is_installed("pillow"):
    launch.run_pip("install pillow", "requirements for objectsAndCaptionsDetection")

if not launch.is_installed("requests"):
    launch.run_pip("install requests", "requirements for objectsAndCaptionsDetection")

if not launch.is_installed("io"):
    launch.run_pip("install io", "requirements for objectsAndCaptionsDetection")

if not launch.is_installed("logging"):
    launch.run_pip("install logging", "requirements for objectsAndCaptionsDetection")

if not launch.is_installed("bs4"):
    launch.run_pip("install bs4", "requirements for objectsAndCaptionsDetection")

if not launch.is_installed("selenium"):
    launch.run_pip("install selenium", "requirements for objectsAndCaptionsDetection")

if not launch.is_installed("seleniumbase"):
    launch.run_pip("install seleniumbase", "requirements for objectsAndCaptionsDetection")

     

# if not launch.is_installed("torch"):
#     launch.run_pip("install torch", "requirements for objectsAndCaptionsDetection")

# if not launch.is_installed("datetime"):
#     launch.run_pip("install datetime", "requirements for objectsAndCaptionsDetection")

# if not launch.is_installed("re"):
#     launch.run_pip("install re", "requirements for objectsAndCaptionsDetection")

# if not launch.is_installed("bitsandbytes"):
#     launch.run_pip("install bitsandbytes>=0.41.3", "requirements for objectsAndCaptionsDetection")

# if not launch.is_installed("accelerate"):
#     launch.run_pip("install accelerate", "requirements for objectsAndCaptionsDetection")

# if not launch.is_installed("peft"):
#     launch.run_pip("install peft", "requirements for objectsAndCaptionsDetection")  
        

    